
baseURL = "https://example.com/"
languageCode = "en-us"
title = "Poetry"
paginate = 10

[params]
  author = "Your Name"

[taxonomies]
  tag = "tags"

# Use relative URLs so the site works correctly when served from a subpath (GitHub Pages)
relativeURLs = true
canonifyURLs = false
